# Local dev: wrangler reads .dev.vars (not .env) for Worker secrets/env
# Copy this to .dev.vars and fill in any values you need locally.

# Optional: override the public app URL used in share links.
# Defaults to the request origin if unset.
# APP_URL=http://localhost:8787
